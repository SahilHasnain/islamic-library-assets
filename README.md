# Islamic Library Assets

This repository stores the public published output for the Islamic Library app.

It is intended to contain only static delivery artifacts:

- `catalog.json`
- per-book `metadata.json`
- per-volume `manifest.json`
- page images
- covers

The app should read these assets through jsDelivr after the worker publishes them.
